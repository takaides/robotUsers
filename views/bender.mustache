<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title>Directory</title>
  <link rel="stylesheet" href="/styles.css">
</head>

<body>
  <section class="wrapperV">
    <!-- significantly increase width -->
    <div class="userH">
      <!-- move picture, add border, set border radius to 50% -->
      <div id="cardHeader">
        <div>
          <img src= {{avatar}}></a>
        </div>
        <div id="cardInfo">
          <h1>{{name}}</h1>
          <p>
            {{#address.city}}{{address.city}}, {{/address.city}}
            {{#address.state_or_province}}{{address.state_or_province}}, {{/address.state_or_province}}{{address.country}} |
            {{#job}}{{job}}, {{company}}</p>
          {{/job}}
          {{^job}}
          <div class="jerb inline">Available for hire</div></p>
          {{/job}}
          <p>{{email}} | {{phone}}</p>
        </div>
      </div>
      <div>
        {{#university}}
          <h3 class="inline">Education</h3>
          <p class="inline">{{university}}</p>
        {{/university}}
        <br>
        <h3 class="inline">Skills</h3>
        <p class="inline">{{skills}}</p>
      </div>
    </div>
    <a href="/">Go Home</a>
  </section>
</body>

</html>
